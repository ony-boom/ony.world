# Last.fm "now playing" widget (home page).
# Static site → these are exposed to the browser. Use a read-only Last.fm API key.
# Get a key at https://www.last.fm/api/account/create
# Leave blank to hide the widget.
PUBLIC_LASTFM_API_KEY=
PUBLIC_LASTFM_USER=
